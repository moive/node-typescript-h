describe("App", () => {
	it("should be true", () => {
		expect(true).toBe(true);
	});
	test("should be 30", () => {
		const n1 = 10;
		const n2 = 20;

		const result = n1 + n2;
		expect(result).toBe(30);
	});
});
